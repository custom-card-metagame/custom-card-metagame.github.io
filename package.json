{
  "name": "ptcg-sim-meta-client",
  "version": "1.5.1.1",
  "description": "Client for PTCG Sim Meta - Pokémon TCG Tabletop Simulator",
  "scripts": {
    "build": "mkdir -p dist && cp -r *.html src dist/"
  },
  "dependencies": {
    "socket.io-client": "^4.7.4"
  },
  "devDependencies": {
    "eslint": "^9.20.0",
    "eslint-config-prettier": "^10.0.1",
    "eslint-plugin-prettier": "^5.2.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}